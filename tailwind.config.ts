module.exports = {
    darkMode: "class",
    content: [
        './src/**/*.{html,js,jsx,tsx}',
    ],
    theme: {
        extend: {
            colors: {
                primary: 'var(--color-primary)',
                ghost: 'var(--color-ghost)',
            },
        },
    },
    plugins: [],
}
